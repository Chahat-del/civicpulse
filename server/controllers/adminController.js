// Authority/admin actions will go here
const getFlaggedIssues = (req, res) => {
  res.json({ message: 'Flagged issues - coming soon' });
};

const removeIssue = (req, res) => {
  res.json({ message: 'Remove issue - coming soon' });
};

module.exports = { getFlaggedIssues, removeIssue };